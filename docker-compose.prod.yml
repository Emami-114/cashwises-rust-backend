version: '3.8'

services:
  caddy:
    image: caddy/caddy:2.7.6-alpine
    container_name: caddy
    restart: unless-stopped
    environment:
      - CF_API_TOKEN=SQuqzPrpfVK-3dy3oGOSys7dtmFQF1nYVQeUh1Pb
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - $PWD/Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - rust_app_network

volumes:
  caddy_data:
  caddy_config:
